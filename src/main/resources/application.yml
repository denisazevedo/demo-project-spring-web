spring:
   application:
      name: demo-web-app

management:
   endpoints:
      web:
         exposure:
            include: "*"
   endpoint:
      shutdown:
         enabled: true
endpoints:
   shutdown:
      enabled: true

server:
  port: 9090
